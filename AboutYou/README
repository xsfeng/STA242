Fill in the HTML file in aboutYou.html.
Make certain to commit any changes with

  git add aboutYou.html  myPic.png
  git commit -m "some message"

If you are using bitbucket.org or github.com,
then push this to the remote repository, e.g.,
  
  git push origin master

Make certain to share the repository with me (duncantl) (via the settings page).


If you are not using a remote repository, create a compressed 
version of this and mail it to me (Duncan).

  cd ../..
  tar zcf stat242_2015.tar.gz stat242_2015

Mail me the stat242_2015.tar.gz
